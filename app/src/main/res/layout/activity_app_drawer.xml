<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/black"
    android:tag="bg=plain:background"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="8dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="[ ALL APPLICATIONS ]"
            android:textColor="@color/neon_cyan"
            android:tag="text=textLabel;bg=borderBox"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:padding="8dp"
            android:background="@drawable/border_box"
            android:layout_marginEnd="4dp" />

        <TextView
            android:id="@+id/themeButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="🎨"
            android:textColor="@color/neon_green"
            android:tag="text=accentPrimary;bg=borderBox"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:padding="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:background="@drawable/border_box"
            android:clickable="true"
            android:focusable="true"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp" />

        <TextView
            android:id="@+id/sortButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="A-Z"
            android:textColor="@color/neon_green"
            android:tag="text=accentPrimary;bg=borderBox"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:padding="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:background="@drawable/border_box"
            android:clickable="true"
            android:focusable="true"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <EditText
        android:id="@+id/searchBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search apps..."
        android:textColor="@color/white"
        android:textColorHint="@color/text_hint"
        android:tag="text=textPrimary;hint=textHint;bg=borderBox"
        android:textSize="12sp"
        android:fontFamily="monospace"
        android:padding="8dp"
        android:background="@drawable/border_box"
        android:layout_marginBottom="8dp"
        android:inputType="text"
        android:imeOptions="actionSearch" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/appList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/border_box"
        android:tag="bg=borderBox"
        android:padding="4dp" />
</LinearLayout>
